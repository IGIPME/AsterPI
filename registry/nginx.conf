server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # 兼容 rspress 的两种输出：目录式（/guide/index.html）与扁平式（/guide.html）
    location / {
        try_files $uri $uri.html $uri/ $uri/index.html =404;
    }
}
