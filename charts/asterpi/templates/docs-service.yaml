apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-docs
  labels:
    app: docs
spec:
  type: {{ .Values.docs.service.type }}
  selector:
    app: docs
  ports:
    - name: http
      port: {{ .Values.docs.service.port }}
      targetPort: 80
