[workspace]
authors = ["SolomonCaptain <scholar@mail.dlut.edu.cn>"]
channels = ["conda-forge"]
name = "AsterPI"
platforms = [
    "linux-64"
]
version = "0.1.0"
preview = ["pixi-build"]
license = "GPL-3.0-or-later"
license-file = "LICENSE"
readme = "README.md"

[workspace.dependencies]

[dependencies]

[feature.linux-64]
platforms = ["linux-64"]

[environments]
default = ["linux-64"]